import OverAccount from 'component/join/OverAccount';
import React from 'react';

const JoinOverAccountContainer = (props) => {

    return(
        <OverAccount />
    )
}

export default JoinOverAccountContainer;