import SetPassword from 'component/auth/sndPassword/SetPassword';
import React from 'react';

const SndPasswordContainer = (props) => {
    
    const onSubmit = () => {
        
    }
    return(
        <SetPassword onSumbit = {onSubmit} />
)
};

export default SndPasswordContainer;