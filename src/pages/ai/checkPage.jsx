import React from "react";
import AiCheckContainer from "container/ai/AiCheckContainer";

const CheckPage = (props) => (
  <div id="wrap" className="ai">
    <AiCheckContainer />
  </div>
);

export default CheckPage;
